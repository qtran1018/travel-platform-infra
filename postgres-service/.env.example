# Copy to .env.secrets (not committed) and fill in strong values for production.
# Local dev uses the defaults baked into docker-compose.yml — no file needed.
#
# Generate strong passwords: openssl rand -base64 32

POSTGRES_SUPERUSER_PASSWORD=
SPLITPUSH_DB_PASSWORD=
TRAVELBIN_DB_PASSWORD=
ITINERARY_DB_PASSWORD=
